import type { Address } from "viem";
import type { SessionActionLike } from "../../../../../account/decorators/buildSessionAction";
import type { BaseMeeClient } from "../../../../../clients/createMeeClient";
import { type ExecuteSignedQuotePayload, type FeePaymentParams, type Trigger } from "../../../../../clients/decorators/mee";
import type { GetFusionQuoteParams } from "../../../../../clients/decorators/mee/getFusionQuote";
import type { EIP7702AuthorizationParams, InstructionLike } from "../../../../../clients/decorators/mee/getQuote";
import type { Validator } from "../../../toValidator";
import type { GrantPermissionResponse } from "../grantPermission";
export type PrepareForPermissionsParams = Omit<GetFusionQuoteParams, "instructions" | "feeToken" | "trigger"> & {
    smartSessionsValidator: Validator;
    additionalInstructions?: InstructionLike[];
    trigger?: Trigger;
    maxPaymentAmount?: bigint;
    redeemer?: Address;
    /** Actions can be a single SessionAction or array of SessionAction (Will be flattened) */
    actions?: SessionActionLike[];
    batchActions?: boolean;
} & FeePaymentParams & EIP7702AuthorizationParams;
/**
 * Returns undefined if there was no need to prepare the superTx
 */
export type PrepareForPermissionsPayload = (ExecuteSignedQuotePayload & {
    sessionDetails?: GrantPermissionResponse;
}) | undefined;
export declare const prepareForPermissions: (client: BaseMeeClient, parameters: PrepareForPermissionsParams) => Promise<PrepareForPermissionsPayload>;
//# sourceMappingURL=prepareForPermissions.d.ts.map