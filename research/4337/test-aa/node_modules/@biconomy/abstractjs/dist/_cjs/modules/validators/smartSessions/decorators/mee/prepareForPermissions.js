"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareForPermissions = void 0;
const account_1 = require("../../../../../account/index.js");
const mee_1 = require("../../../../../clients/decorators/mee/index.js");
const execute_1 = require("../../../../../clients/decorators/mee/execute.js");
const executeFusionQuote_1 = require("../../../../../clients/decorators/mee/executeFusionQuote.js");
const getFusionQuote_1 = require("../../../../../clients/decorators/mee/getFusionQuote.js");
const prepareForPermissions = async (client, parameters) => {
    const { batchActions = true } = parameters;
    const meeVersions = client.account.deployments.map(({ version, chain }) => ({
        chainId: chain.id,
        version
    }));
    const installInstructions = await (0, mee_1.prepareInstallSmartSessions)(client, parameters.smartSessionsValidator.address);
    const hasInstallInstructions = installInstructions.length > 0;
    const enableSessionsInstructions = [];
    const sessionDetailsArray = [];
    if (parameters.redeemer && parameters.actions) {
        const enableSession = {
            redeemer: parameters.redeemer,
            actions: parameters.actions,
            maxPaymentAmount: parameters.maxPaymentAmount,
            batchActions: parameters.batchActions
        };
        const enableSessionsInstructionsWithSessionDetails = await (0, mee_1.prepareEnableSessions)(client, enableSession, parameters.smartSessionsValidator.address, parameters.feeToken);
        for (const { instructions, sessionDetails } of enableSessionsInstructionsWithSessionDetails) {
            sessionDetailsArray.push(sessionDetails);
            enableSessionsInstructions.push(...instructions);
        }
    }
    const hasEnableSessionsInstructions = enableSessionsInstructions.length > 0;
    if (hasInstallInstructions ||
        hasEnableSessionsInstructions ||
        parameters.additionalInstructions ||
        parameters.trigger) {
        const unresolvedInstructions = parameters.additionalInstructions
            ? [...installInstructions, ...parameters.additionalInstructions]
            : installInstructions;
        const resolvedInstructions = await (0, account_1.resolveInstructions)(unresolvedInstructions);
        let partiallyBatchedInstructions = [];
        let batch = parameters.batch ?? true;
        if (batch) {
            partiallyBatchedInstructions = await (0, account_1.batchInstructions)({
                accountAddress: client.account.signer.address,
                meeVersions,
                instructions: [...resolvedInstructions]
            });
        }
        else {
            partiallyBatchedInstructions = [...resolvedInstructions];
        }
        const instructions = hasEnableSessionsInstructions
            ? [...partiallyBatchedInstructions, ...enableSessionsInstructions]
            : partiallyBatchedInstructions;
        const isUnbatchActionsRequired = !batchActions && hasEnableSessionsInstructions;
        batch = isUnbatchActionsRequired ? false : batch;
        if (parameters.trigger) {
            const quote = await (0, getFusionQuote_1.getFusionQuote)(client, {
                ...parameters,
                instructions,
                batch,
                trigger: parameters.trigger,
                simulation: parameters.simulation
            });
            const { hash } = await (0, executeFusionQuote_1.executeFusionQuote)(client, {
                fusionQuote: quote,
                companionAccount: client.account
            });
            return {
                hash,
                ...(sessionDetailsArray.length > 0
                    ? { sessionDetails: sessionDetailsArray }
                    : {})
            };
        }
        const { hash } = await (0, execute_1.execute)(client, {
            ...parameters,
            batch,
            instructions,
            simulation: parameters.simulation
        });
        return {
            hash,
            ...(sessionDetailsArray.length > 0
                ? { sessionDetails: sessionDetailsArray }
                : {})
        };
    }
    return undefined;
};
exports.prepareForPermissions = prepareForPermissions;
//# sourceMappingURL=prepareForPermissions.js.map